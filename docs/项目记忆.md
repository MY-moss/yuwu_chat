# 云雾酒馆 — 项目记忆

> **⚠️ 同步规则**：所有 AI 共同使用的项目记忆快照。每次修改后必须更新。详细问题请查看「问题反馈与报告.md」，架构规范请查看「项目架构和相关规范.md」。

---

## 一、项目定位

- **名称**: 云雾酒馆 AI Chat & RPG
- **技术栈**: Flask + SQLite + PyInstaller
- **端口**: 9000
- **当前版本**: v2.1.5
- **主要功能**: AI 聊天、跑团（RPG）、模型管理、用户反馈
- **AI API**: opencode.ai/zen/free, model=mimo-v2.5-free, key=public

---

## 二、关键路径

| 路径 | 用途 |
|------|------|
| `src/backend/app.py` | Flask 主应用（2634行，22区块分区注释） |
| `src/backend/static/script.js` | 主前端逻辑（2654行，18区块分区注释） |
| `src/backend/static/style.css` | 样式表（2600行，9区块分区注释） |
| `src/backend/static/feedback.js` | 反馈模块（391行，9区块分区注释） |
| `src/backend/static/feedback.css` | 反馈页样式（352行，6区块分区注释） |
| `src/backend/templates/index.html` | 主页面（529行） |
| `src/backend/templates/dashboard.html` | 管理面板（297行） |
| `docs/代码分类索引.md` | 代码区块分类导航（v2.1.5新增） |
| `启动.bat` | 根目录一键启动脚本（v2.1.5新增） |
| `src/backend/instance/tavern.db` | SQLite 开发数据库 |
| `release/` | 发布版（含 tavern.exe 16MB，已删除 app.py） |
| `scripts/` | 8个工具脚本（均已修复路径） |

---

## 三、最近变更摘要

| 日期 | 变更 |
|------|------|
| 2026-07-05 | **第32轮（代码逻辑分类重组 v2.1.5）**：5个源码文件添加统一分区注释（app.py=22区块/script.js=18/style.css=9/feedback.js=9/feedback.css=6，合计64区块）；新建 docs/代码分类索引.md 和根目录 启动.bat；删除 release/app.py；更新架构文档和项目记忆。 |
| 2026-07-05 | **第26-35轮（兼容性+UI+代码质量）**：第35轮兼容性与UI修复(L15/M62/M63/L57/L60+L33部分maxlength)。第34轮(L20/L22/L36/L37)。第33轮(L17/L55/M56/L7)。第32轮(L16/M53/M54/M55/L29)。第31轮(L2/L3/L23/L24/L28/L31/L32)。第30轮(H13/H8/M33)。第29轮(M18/M22/M38/H7/H16/M19)。第28轮(H29-H32+M67-M74)。第25轮(删9调试脚本)。第24-21轮。问题数101→90→87→84→77→73→71→67→63。 |
| 2026-07-04 | **第11-28轮修复（合并）**：第28轮UI大批修复(H29-H32四项P0+M67-M74七项P1：header布局、移动端响应式、RPG三栏、时间戳、多行输入、流式反馈、世界书卡片、选项按钮、密码可见性)。第27轮API配置迁移至模型级别、聊天/跑团切换tab同步、模型编辑、故事加载进度条、分享修复、移除字号调整。第25轮聊天/跑团切换UI修复、自有API配置重构、usage_log格式修复。第24轮(H2流式Token计数→estimate_tokens、H3登录速率限制、H7/H8 branch_session问题、H15公开枚举→登录限制、H18 setInterval清理)。第23轮(H1 histories加锁、H9 chat孤儿消息→延迟写入、H17 catch{}加错误日志、H28 CDN SRI校验)。第22轮(H5 reset_db弱密码→随机密码、H6 rpg_sessions加锁、H11 API错误回传→隐藏详情、H27 target_blank安全)。第21轮(CSRF保护、escape_html转义、CSRF token；审计发现M65/M66/L50-L53共6项；修复call_ai/load_agents/load_worlds缺少try/except；改进评估脚本自动扫描)。第19-20轮复核清洗、Critical归零、XSS修复、scripts/tavern/release审计。第11-18轮文档重组、10项Critical修复、4Agent深度审计(163项)、XSS全面修复(50处)、SSRF防护、代码清理、创建AI行为评估脚本。问题数56→106 |
| 2026-07-03 | 目录重构：docs/、scripts/、src/backend/ 分离 |

---

## 四、待办优先级 Top5

| # | 优先级 | 问题 | 文件 | 说明 |
|---|--------|------|------|------|
| 1 | 🟠 P1 | H11: 上游API错误吞没 | app.py | 仅返回通用错误消息 |
| 2 | 🟠 P1 | M21: admin可删唯一管理员 | app.py | 系统锁死风险 |
| 3 | 🟠 P2 | H23: bump_version.ps1破坏version.json | scripts/ | 默认show模式破坏version.json |
| 4 | 🟠 P2 | H24: release/启动脚本路径无效 | release/ | python回退路径无效 |
| 5 | 🟠 P2 | H26: requests失败僵尸进程 | scripts/ | 启动脚本requests失败产生僵尸进程 |

## 五、已知风险 Top3

| # | 风险 | 可能性 | 影响 | 说明 |
|---|------|--------|------|------|
| 1 | upstream错误吞没 | 中 | 低 | H11: 调试困难 |
| 2 | admin可删唯一管理员 | 低 | 严重 | M21: 误删最后一个管理员锁死系统 |

## 六、AI 接手规则

### 6.0 执行前必读文档（与项目架构和相关规范.md同等地位）
1. **项目架构和相关规范.md** — 架构规范、修改规则
2. **AI行为评估脚本.py** — 评估标准，规范AI行为
3. **项目记忆.md** — 项目快照，待办优先级
4. **问题反馈与报告.md** — 现存问题清单

### 6.1 开始工作前必做
1. 阅读本文件 — 了解项目状态和待办事项
2. 阅读「项目架构和相关规范.md」— 理解目录结构和修改规则
3. 阅读「问题反馈与报告.md」— 了解当前待修复问题（63项）

### 6.2 修改代码时必遵守
1. **唯一修改入口** — 所有代码修改必须在 `src/backend/` 目录进行
2. **同步规则** — 修改完成后同步到 `release/`（前端文件及数据文件）
3. **禁止操作** — 直接在 `release/`中修改代码

### 6.3 修改完成后必检查
1. **版本同步** — `version.json` 和 `CHANGELOG.json` 是否一致
2. **安全检查** — 新增的 API 路由是否有正确的认证装饰器
3. **文档更新** — 按顺序更新三个文档

---

## 七、文档同步顺序

每次审计或修改后，必须按以下顺序更新文档：
1. **问题反馈与报告.md** — 先更新问题清单
2. **项目架构和相关规范.md** — 更新版本历史、同步状态
3. **项目记忆.md** — 更新待办优先级、风险、最近变更
---
**项目记忆生成**: 2026-07-05 | **审计轮次**: 第35轮兼容性与UI修复 | **现存问题**: 63个 | **最新变更**: 第35轮修复4项(L15/M62/M63/L57/L60) + L33部分(maxlength 17+)
