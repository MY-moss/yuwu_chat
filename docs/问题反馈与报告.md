# 云雾酒馆 — 问题反馈与报告

> **⚠️ 同步规则**：所有问题的权威来源。每次审计发现新问题或修复后，必须先更新本文件，再同步更新「项目架构和相关规范.md」和「项目记忆.md」。

---

## 一、问题统计

**审计日期**: 2026-07-04 | **审计轮次**: 第19轮复核清洗  
**当前代码行数**: app.py≈2160, script.js=2270, feedback.js=324, style.css≈2300  
**累计发现**: ~270个 | **已修复**: 143个 | **现存**: **127个**

| 级别 | 现存 | 说明 |
|------|:----:|------|
| 🔴 Critical | 0 | ✅ 全部修复（6项XSS+SSRF+TOCTOU已全部确认修复） |
| 🟠 High | 29 | 功能损坏、数据丢失、信息泄露（新增H29: rpg_act/start_rpg缺try/except） |
| 🟡 Medium | 56 | 边界情况、并发风险、代码异味（新增M64: reset_admin_password返回密码） |
| 🔵 Low | 42 | 代码质量、一致性、规范问题 |
| **总计** | **127** | |

> **第19轮复核**: 逐项验证C1-C6全部确认修复。修复C3(玩家名XSS→输入校验)、C4(feedback.js username→escapeHtml)、C5(SSRF→is_safe_url校验)、C6(TOCTOU→SQL原子更新)、C1/C2(所有innerHTML已使用escapeHtml)。新增H29、M64。现存Critical归零。

---

## 二、🔴 Critical（致命问题 — 0项）

✅ **所有致命问题已全部修复。** 历史记录见已修复问题列表。

---

## 三、🟠 High（严重问题 — 29项）

### app.py（17项）

H1. `histories` 全局字典无锁保护（app.py:115,843-849）  
H2. 流式 Token 计数 `len(full_text) // 2`（app.py:1596）  
H3. 登录 API 无速率限制（app.py:544-559）  
H4. CSRF 无保护  
H5. `scripts/reset_db.py` 硬编码弱密码  
H6. `rpg_sessions` 全局字典无读写锁保护  
H7. branch_session 返回的 relationships 恒为空字典（app.py:1773）  
H8. branch_session 历史截断 off-by-one（app.py:1757-1764）  
H9. chat 在调用 API 前就 append 用户消息，失败后残留孤儿消息（app.py:851,856-901）  
H10. 所有 JSON 文件写入非原子（app.py 多处: 140-141,171-172,188-189,202-203,228-229,424-425）  
H11. chat/call_ai 将上游 API 错误正文回传给用户（app.py:871,901,928）  
H12. histories 字典永不截断/淘汰（内存泄漏）  
H13. save_sessions 静默截断 history 并丢弃超过100个会话（app.py:134,138-139）  
H14. load_agents/worlds/ratings/submissions 无 try/except（app.py:417-420,174-184,162-167,192-197）  
H15. list_shared_sessions 无需登录公开枚举所有分享对局（app.py:1284-1303）  
H16. session_id 仅取 uuid4 前8位无碰撞检测（app.py:1351）  
**H29. start_rpg/rpg_act 调用 call_ai 无 try/except（app.py:1365,1453）**  
- **问题**: chat() 有 try/except 包裹 API 调用，但 RPG 流程没有。AI 接口失败时用户收到原始 500 错误，丢失操作

### script.js（6项）

H17. `catch {}` 静默吞异常（script.js 约14处）  
H18. `setInterval` 离开页面后不清理（script.js:2081,2122,2163）  
H19. `renderStatus` AI 内容直接 `innerHTML`（script.js:950-970）  
H20. `renderSectionsHtml`/`parseStatusValue` 注入 section 键值未转义（script.js:945,947,977-992,2204-2206）  
H21. 管理员"会话"面板 `state_preview` 原样注入（script.js:2044）  
H22. AI 输出 XSS：`cleanText` 不做 HTML 转义（script.js:864-869,1200-1201）

### scripts（4项）

H23. bump_version.ps1 默认 show 模式会破坏 version.json  
H24. release/ 启动脚本 python 回退路径无效  
H25. tavern/start.bat 无 python 回退  
H26. Python 启动脚本在 requests 失败时产生僵尸进程

### index.html（2项）

H27. target="_blank" 缺少 rel="noopener"（index.html:333）  
H28. CDN 脚本缺少 SRI 完整性校验（index.html:560-561, spectate.html:45-46）

---

## 四、🟡 Medium（中等问题 — 56项）

### app.py（32项）

M1. `histories` 字典无上限（内存泄漏）  
M2. JSON 文件无缓存 — N+1 重复读取  
M3. 多处 `int()/float()` 无异常保护（app.py:1040,1103-1104,1697,1740,1809-1810,1905-1906,1991）  
M4. `bump_version.ps1` 不更新 CHANGELOG  
M6. `credits_per_1k` 无类型验证（app.py:670）  
M9. `spectate.html:48` Jinja2 变量直接插值到 JS  
M10. `style.css` 11 处硬编码颜色未使用 CSS 变量  
M11. `feedback.js:157` API 错误消息直接 innerHTML  
M16. `rpg_sessions` 无 TTL/清理机制  
M17. 反馈搜索存在 SQL 通配符注入（app.py:2019-2021）  
M18. admin_update_user 不校验 role 值（app.py:756-761）  
M19. admin_update_user 不校验 credits 范围（app.py:757）  
M20. admin_delete_user 不清理关联数据（app.py:765-777）  
M21. admin_delete_user 可删除唯一管理员导致锁死（app.py:765-777）  
M22. user_api_config PUT 不校验 api_base URL scheme（app.py:641-649）  
M23. submit_world 不校验 temperature/max_tokens 范围（app.py:1103-1104）  
M24. rate_world 不校验 world_id 是否存在（app.py:1035-1069）  
M25. rate_world 对 ratings 文件存在 TOCTOU 竞态（app.py:1045-1069）  
M26. register 不校验用户名长度/格式（app.py:527-537）  
M27. register check-then-insert 竞态（app.py:536-542）  
M28. list_feedback 不限 per_page 上限（app.py:2004-2005）  
M29. list_feedback 不校验 page/per_page 下界（app.py:2004-2005,2025）  
M30. get_current_user 用 GET 修改状态并每次写库（app.py:605-611）  
M31. change_password 不失效其他会话（app.py:581-602）  
M32. rpg_act_stream 在生成器内做 db.session.commit（app.py:1606）  
M33. rpg_act_stream 客户端断流导致状态半更新（app.py:1617-1624）  
M34. 大量 db.session.commit() 无 try/except（22处）  
M35. feedback category/rating 未校验范围（app.py:1990-1991）  
M36. add_agent 不校验 agent 结构与必填字段（app.py:786-796）  
M37. active_count 暴露全局活跃对局数（app.py:1010-1023）  
M38. admin_add_model/update_model 不校验 api_base URL scheme（app.py:688-689,714-718）  
**M64. reset_admin_password 返回新密码到响应体（app.py:583）**  
- **问题**: 新密码明文返回到 API 响应中，虽受 @admin_required 保护，但违反了密码不返回的最佳实践

### script.js（9项）

M40. 登录/注册/登出/改密无 try/catch  
M41. `api()` helper 在非 JSON 响应上崩溃（script.js:360-373）  
M42. 原始 fetch 调用绕过"未登录"重定向逻辑  
M43. `sendMessage` Enter 键竞态  
M44. `actGame` 无重入保护  
M45. `resumeGame` 不恢复 `rpgState.sections`  
M46. `actGame` 流式结束 loading 蒙层卡死  
M47. `jumpToRound`/`startGame`/`resumeGame` 无 try/catch  
M48. 流式循环 `JSON.parse` 未保护

### 前端模板/样式（15项）

M49. feedback.js 提交按钮网络异常后永久禁用  
M50. feedback.js init() 无 try/catch  
M51. feedback.css 大量硬编码颜色  
M52. index.html 内联事件处理器 onchange（CSP 不兼容）  
M53. index.html label 未关联 input  
M54. index.html 密码输入框缺少 autocomplete  
M55. feedback.html label 未关联表单控件  
M56. feedback.html modal 缺少 ARIA 属性  
M57. dashboard.html updateWorldStats 缺少 401 检查  
M58. dashboard.html 内联 style 与 script 块（CSP 不兼容）  
M59. dashboard.html :root 变量与 style.css 不一致  
M60. spectate.html 会话结束后未停止轮询  
M61. spectate.html d.world 未做空值检查  
M62. style.css backdrop-filter 未加 -webkit- 前缀  
M63. style.css 全局缺少 @media (prefers-reduced-motion)

---

## 五、🔵 Low（低优先级问题 — 42项）

### app.py（19项）

L1. `api()` helper 与直接 `fetch` 混用  
L2. Toast 超时硬编码 2500ms  
L3. `feedback.js` toast 无 `toastContainer` 空值检查  
L4. `feedback.js` 使用 `$()` 和 `document.getElementById` 不一致  
L7. `style.css` Google Fonts CDN 依赖  
L8. `!important` 使用 3 处  
L10. `feedback.js` onclick 和 addEventListener 混用  
L11. init_db 使用双引号字符串字面量写 SQL  
L12. Feedback.user_id、User.last_active 缺索引  
L13. get_effective_api 内部重复 import current_user  
L14. save_sessions 使用 default=str 掩盖序列化错误  
L15. load_sessions 用 str(id(s)) 作 session_id 兜底  
L16. rpg_act_stream/rpg_act 历史截断保留 29 条而非 30 条  
L17. parse_rpg_reply 仅匹配中文【】括号  
L18. delete_agent 仅清理当前管理员自己的 histories  
L19. feedback_stats 中 ratings.count(i) 对每个 i 全表扫描  
L20. generate_credit_key 逐条 add 而非批量  
L21. 无数据库迁移机制  
L22. update_api_config_route 用 "***" 子串判断是否更新

### script.js（12项）

L23. deleteWorld 重置 rpgState 缺键  
L24. doSkillCheck setTimeout 竞态  
L25. 轮次计数不一致  
L26. reviewRound 局部变量遮蔽全局 storyText  
L27. populateModels 失败回退用可能不存在的 model id  
L28. formatTime 对非法输入返回 "Invalid Date"  
L29. 顶层 addEventListener 假定所有 DOM 元素存在  
L30. 世界排序/删除采用"取全部→改→PUT"竞态  
L31. logout 不重置应用状态  
L32. genKeyBtn/worldEditorSave 等无 try/catch  
L33. 输入字段无客户端长度/字符限制  
L34. loadAgents/renderAgentList 未做 null 校验

### 前端模板/样式（6项）

L35. feedback.js parseInt 未指定 radix  
L36. feedback.js 使用 confirm() 阻塞式对话框  
L37. feedback.js escapeHtml 每次调用创建 DOM 元素  
L38. feedback.js 弹窗缺少焦点管理与键盘交互  
L39. dashboard.html "加载中..." 在 fetch 失败后永久停留  
L40. dashboard.html setInterval 从未清除

### 第17轮新发现（5项）

L41. script.js 8处空catch块静默吞异常（行90/384/503/545/597/1452/1816/2190）  
L42. script.js 第2213行无用注释 "Show share button (now handled inside startGame/resumeGame)"  
L43. app.py 3处重复的关系解析逻辑（start_rpg/rpg_act/rpg_act_stream 各一份相同代码）  
L44. app.py chat() 与 call_ai() API调用逻辑高度相似，可合并  
L45. tavern/ 目录与 release/ 启动脚本功能重叠，可能冗余

---

## 六、已修复问题（新增6项Critical+8项P0+历史修复）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| C1(旧) | 数据字段注入XSS(约14处) | 所有 innerHTML 拼接已使用 escapeHtml 或 renderMarkdown(DOMPurify) |
| C2(旧) | 观战XSS | spectate.html + script.js 内联观战均已使用 escapeHtml |
| C6/C7(旧) | SSRF: personal_api_base 无校验 | 添加 is_safe_url() 过滤内网IP(10.x/192.168/172.16-31/localhost) |
| C7(旧) | redeem_key TOCTOU 双花 | 改为 SQL 原子 UPDATE + affected rows 检查 |
| C3(第19轮) | onclick属性注入(26处) | 改为addEventListener绑定 |
| C4(第19轮) | DOMPurify CDN失败无回退 | 添加escapeHtml净化 |
| C5(第19轮) | feedback.js escapeHtml不转义引号 | 改用显式字符替换 |
| C10(第19轮) | textarea闭合注入 | 添加escapeHtml转义 |
| H1(第19轮) | 个人API密钥明文返回 | 删除响应中的api_key字段 |
| H7(第19轮) | 硬编码密码123456 | 改为环境变量+随机生成 |
| H22(第19轮) | admin_list_models明文返回api_key | to_dict_admin改为has_api_key |
| H23(第19轮) | get_api_config_route明文返回API_KEY | 返回***替代 |
| C3(第19轮) | 玩家名存储型XSS | startGame添加输入校验.replace(/[<>"'&]/g, '') |
| C4(第19轮) | feedback.js列表页username XSS | 添加escapeHtml(fb.username) |
| C8(历史) | rpg_act_stream 免费模型扣1积分 | 计费逻辑修正 |
| C9(历史) | rpg_act_stream 缺失 mock 回退 | 添加降级处理 |
| C13(历史) | dashboard.html 4处XSS | escapeHtml 修复 |
| C14(历史) | spectate.html 3处XSS | escapeHtml + DOMPurify |
| H11(历史) | 无 HTTP 错误处理器 | 添加 404/500 handler |
| H12(历史) | 重复 SQL 查询 | 合并为一次查询 |
| H13(历史) | deduct_credits 无异常保护 | 添加 try/except |
| H14(历史) | 启动时 API 密钥前缀输出 | 移除 print |
| H16(历史) | update_session 未持久化 priority | 添加 save_sessions |
| M5(历史) | _agents_lock 未使用 | 加锁实现 |
| M7(历史) | 登录密码无 .strip() | 添加 strip |
| M8(历史) | spectate.html AI 内容 innerHTML 未净化 | 添加 escapeHtml |
| M12(历史) | API_KEY/API_URL 死代码 | 删除冗余变量 |
| M13(历史) | load_feedback 无锁 | 添加锁 |
| M14(历史) | sections_get 死函数 | 删除 |
| M15(历史) | submit_world 无异常保护 | 添加 try/except |
| M39(历史) | load_feedback 死代码 | 删除 |
| L5(历史) | TEMPLATES_AUTO_RELOAD | 设为 False |
| L6(历史) | 缓存头仅 js/css | 扩充至全资源 |
| L9(历史) | 缺少安全响应头 | 添加 X-Content-Type-Options 等 |

---

## 七、修复优先级

### P1 — 功能损坏（应尽快修复）
| # | 问题 |
|---|------|
| H7, H8 | branch_session 逻辑错误 |
| H9 | chat 孤儿消息 |
| H10 | JSON 写入非原子 |
| H13 | save_sessions 静默截断 |
| H23-H26 | scripts/启动脚本问题 |
| H29 | rpg_act/start_rpg 无 try/except |

### P2 — 安全加固（可计划修复）
| # | 问题 |
|---|------|
| H1, H11 | 信息泄露(histories无锁/上游错误回传) |
| H3, H4 | 鉴权加固(速率限制/CSRF) |
| H27, H28 | 资源治理(target=blank/CDN SRI) |

### P3 — 代码质量（后续优化）
| # | 问题 |
|---|------|
| M64 | reset_admin_password 返回密码 |
| M17 | 反馈搜索SQL通配符 |
| H14, M34 | 异常处理不足 |

---

**问题报告生成**: 2026-07-04 | **第19轮复核清洗** | **Critical归零** | **新增H29/M64** | **现存: 127个**
