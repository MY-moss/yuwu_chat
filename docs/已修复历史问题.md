# 云雾酒馆 — 已修复历史问题

> **⚠️ 同步规则**：本文件记录所有已修复问题的历史。从 `问题反馈与报告.md` 中剥离独立维护。活跃问题请查看 `问题反馈与报告.md`。

---

**累计修复**: 250个 | **最后一个修复轮次**: 第35轮（兼容性与UI修复）

---

## 历史修复（早期问题）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| C1 | 数据字段注入XSS(约14处) | 所有 innerHTML 拼接已使用 escapeHtml 或 renderMarkdown(DOMPurify) |
| C2 | 观战XSS | spectate.html + script.js 内联观战均已使用 escapeHtml |
| C3 | onclick属性注入(26处) | 改为addEventListener绑定 |
| C4 | DOMPurify CDN失败无回退 / feedback.js列表页username XSS | 添加escapeHtml净化 + escapeHtml(fb.username) |
| C5 | feedback.js escapeHtml不转义引号 / 玩家名存储型XSS | 改用显式字符替换 + startGame添加输入校验 |
| C6/C7 | SSRF: personal_api_base 无校验 / redeem_key TOCTOU双花 | 添加is_safe_url()过滤内网IP + SQL原子UPDATE |
| C8 | rpg_act_stream 免费模型扣1积分 | 计费逻辑修正 |
| C9 | rpg_act_stream 缺失 mock 回退 | 添加降级处理 |
| C10 | textarea闭合注入 | 添加escapeHtml转义 |
| C13 | dashboard.html 4处XSS | escapeHtml 修复 |
| C14 | spectate.html 3处XSS | escapeHtml + DOMPurify |
| H1 | 个人API密钥明文返回 / histories字典无锁保护 | 删除响应中api_key字段 + 添加_histories_lock |
| H2 | 流式Token计数 len/2 | 改用estimate_tokens()中文+英文加权算法 |
| H3 | 登录无速率限制 / CSRF无保护 | 添加_LOGIN_RATE_LIMIT=5 + csrf_protect() |
| H4 | CSRF 无保护 | 添加csrf_protect() before_request + X-CSRF-Token校验 |
| H5 | reset_db.py 硬编码弱密码 | 改用secrets.token_urlsafe(12)随机生成 |
| H6 | rpg_sessions 无读写锁 | 添加_sessions_lock，所有操作加锁 |
| H7 | 硬编码密码123456 | 改为环境变量+随机生成 |
| H9 | chat 孤儿消息残留 | user+assistant均在API成功后写入histories |
| H10 | JSON 写入非原子 | 统一使用atomic_json_dump() tempfile+shutil.move模式 |
| H11 | 无 HTTP 错误处理器 | 添加404/500 handler |
| H12 | 重复 SQL 查询 | 合并为一次查询 |
| H13 | deduct_credits 无异常保护 | 添加try/except |
| H14 | 启动时 API 密钥前缀输出 | 移除print |
| H15 | list_shared_sessions 公开枚举 | 添加@login_required装饰器 |
| H16 | update_session 未持久化 priority | 添加save_sessions |
| H22 | admin_list_models明文返回api_key | to_dict_admin改为has_api_key |
| H23 | get_api_config_route明文返回API_KEY | 返回***替代 |
| H25 | tavern/start.bat 无 python 回退 | 添加python ../src/backend/app.py作为最终回退 |
| H28 | CDN 缺少 SRI 校验 | marked.min.js + purify.min.js添加integrity + crossorigin |
| M5 | _agents_lock 未使用 | 加锁实现 |
| M7 | 登录密码无.strip() | 添加strip |
| M8 | spectate.html AI 内容 innerHTML 未净化 | 添加escapeHtml |
| M12 | API_KEY/API_URL 死代码 | 删除冗余变量 |
| M13 | load_feedback 无锁 | 添加锁 |
| M14 | sections_get 死函数 | 删除 |
| M15 | submit_world 无异常保护 | 添加try/except |
| M39 | load_feedback 死代码 | 删除 |
| L5 | TEMPLATES_AUTO_RELOAD | 设为False |
| L6 | 缓存头仅js/css | 扩充至全资源 |
| L9 | 缺少安全响应头 | 添加X-Content-Type-Options等 |

## 第20轮修复

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| H29 | start_rpg/rpg_act无try/except | 添加try/except包裹call_ai调用 |
| M64 | reset_admin_password返回密码 | 改为日志输出，不返回密码到响应体 |
| L46 | bump_version脚本路径错误 | version.json路径修正为src/backend/ |
| L47 | tavern启动脚本找不到exe | 修正exe检查路径为../release/ |
| L48 | launch.py隐藏错误输出 | 改为输出到stdout并实时显示 |
| L49 | launch.py无异常保护 | 添加try/except包裹requests调用 |

## 第21轮修复

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| H3 | CSRF 无保护 | 添加CSRF token机制，script.js/feedback.js添加token发送 |
| — | escape_html 转义函数 | 添加统一escape_html函数 |
| — | 评估脚本误报 | 修复AI行为评估脚本.py的false positive问题 |

## 第26轮修复（全面安全加固）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| H1 | histories字典无锁保护 | 添加_histories_lock，所有读写操作加锁 |
| H2 | 流式Token计数len/2 | 改用estimate_tokens()中文+英文加权算法 |
| H3 | 登录无速率限制 | 添加_LOGIN_RATE_LIMIT=5 + 60秒滑动窗口 |
| H4 | CSRF无保护 | 添加csrf_protect() before_request + X-CSRF-Token校验 |
| H5 | reset_db.py硬编码弱密码 | 改用secrets.token_urlsafe(12)随机生成 |
| H6 | rpg_sessions无读写锁 | 添加_sessions_lock，所有操作加锁 |
| H9 | chat孤儿消息残留 | user+assistant均在API成功后写入histories |
| H10 | JSON写入非原子 | 统一使用atomic_json_dump() tempfile+shutil.move模式 |
| H15 | list_shared_sessions公开枚举 | 添加@login_required装饰器 |
| H25 | tavern/start.bat无python回退 | 添加python ../src/backend/app.py作为最终回退 |
| H28 | CDN缺少SRI校验 | marked.min.js + purify.min.js添加integrity + crossorigin |

## 第27轮修复（文档与量化评估）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| M73 | 字号控制按钮位置不合理 | 移除字体大小调整功能（删除A-/A/A+按钮、CSS类、JS事件监听器） |

## 第28轮修复（UI/UX优化）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| H29 | 顶部工具栏按钮拥挤溢出 | header重构为两行布局：header-top(logo+用户身份)、header-bottom(控件+操作按钮) |
| H30 | 移动端header布局错位 | 768px以下header-top/bottom改为column，控件和按钮各自居中 |
| H31 | 模型选择器位置不直觉 | model-select与mode-tabs合并到header-controls组 |
| H32 | RPG三栏布局中小屏幕崩溃 | 添加1024px/768px/480px三档响应式：缩窄→堆叠→压缩高度 |
| M67 | 聊天消息缺少时间戳和头像 | appendMessage添加msg-header，含msg-avatar、msg-name、msg-time |
| M68 | 聊天输入框只支持单行 | input改为textarea，Enter发送/Shift+Enter换行，input事件自动调整高度 |
| M69 | 缺少消息发送流式反馈 | sendMessage添加thinking-dots三点动画，AI回复后替换内容 |
| M71 | 世界书卡片信息密度不均匀 | .wc-rating改用gap+flex-wrap，rating-text弹性填充，rate-btn flex-shrink |
| M72 | 跑团选项按钮排列不佳 | .choices-area改用CSS Grid auto-fit minmax(140px,1fr) |
| M74 | 登录/注册缺少密码可见性切换 | 3处密码框添加.pwd-toggle按钮，点击切换type，添加autocomplete |

## 第29轮修复（安全加固与并发修复）

| 编号 | 问题 | 维修内容 |
|------|------|----------|
| M18 | admin_update_user不校验role值（提权风险） | 添加role值白名单校验(user/admin)，管理员数量上限3人 |
| H7 | branch_session relationships恒为空 | 在分支会话时重新解析关系_map，确保relationships字段正确 |
| M22 | api_base URL scheme未校验（SSRF） | 在user_api_config、update_user_model中添加is_safe_url()校验 |
| M38 | admin model URL scheme未校验 | 在admin_add_model、admin_update_model中添加is_safe_url()校验 |
| H16 | session_id碰撞风险 | 从uuid4前8位改为前12位，大幅降低碰撞概率 |
| M19 | credits范围未校验 | 在admin_update_user和generate_credit_key中添加0-999999范围校验 |

## 第30轮修复（数据完整性与并发修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **H13** | save_sessions 静默截断 — 超100会话数据静默丢失 | 按创建时间排序保留最近500会话，删除旧会话前打印日志警告 |
| **H8** | branch_session off-by-one — 重复 assistant 回复 | 修复历史截断逻辑，移除末尾多余的assistant消息 |
| **M33** | rpg_act_stream 断流状态半更新 | 使用temp_history临时存储，成功后再更新sess["history"] |

## 第31轮修复（UI/UX问题修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **L28** | formatTime 对非法输入返回 "Invalid Date" | 添加 isNaN 检查，非法日期返回空字符串 |
| **L31** | logout 不重置应用状态 | 重置 rpgState、currentMode、isShared 等状态，清理定时器和UI内容 |
| **L23** | deleteWorld 重置 rpgState 缺键 | 补充 storyline 键，保持状态对象结构一致 |
| **L24** | doSkillCheck setTimeout 竞态 | 添加 skillCheckTimer 变量，执行前清除旧定时器 |
| **L32** | genKeyBtn/worldEditorSave 无 try/catch | 添加 try/catch 异常处理，捕获错误并提示用户 |
| **L3** | feedback.js toast 无空值检查 | 添加 msg 空值检查，避免显示空 toast |
| **L2** | Toast 超时硬编码 2500ms | 提取为常量 TOAST_DURATION（3000ms），便于统一管理 |

## 第32轮修复（代码质量与UI修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **L16** | 历史截断保留 29 条而非 30 条 | 将保留条数从 29 改为 30，两处截断逻辑同步修正 |
| **M53** | index.html label 未关联 input | 为修改密码弹窗内 3 个 label 添加 for 属性 |
| **M54** | index.html 密码框缺少 autocomplete | 为修改密码弹窗 3 个输入框添加 autocomplete 属性 |
| **M55** | feedback.html label 未关联表单控件 | 为分类/标题/详细描述 3 个 label 添加 for 属性 |
| **L29** | 顶层 addEventListener 假定 DOM 元素存在 | loginBtn/registerBtn/logoutBtn 改为 ?. 可选链调用 |

---

## 第33轮修复（代码风格与UI修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **L17** | parse_rpg_reply 仅匹配中文【】括号 | 正则扩展支持【】（）[] ()，兼容各LLM输出格式 |
| **L55** | Toast 消失过快 | 延长显示时长 2500ms→3500ms |
| **M56** | feedback.html modal 缺少 ARIA 属性 | 添加 role="dialog" aria-modal="true" 增强可访问性 |
| **L7** | style.css Google Fonts CDN 依赖 | Noto Sans SC 补充 Microsoft YaHei/PingFang SC/Hiragino Sans GB/WenQuanYi Micro Hei 回退；Noto Serif SC 补充 SimSun/Noto Serif CJK SC 回退；font-display:swap 已内建 |

---

## 第34轮修复（代码风格与UI修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **L20** | generate_credit_key 逐条 add 而非批量 | 改为列表推导 + db.session.add_all() 批量提交 |
| **L22** | update_api_config_route "***" 子串判断 | 改为 `!= '********'` 全等判断，与其他API配置路由一致；避免虚假匹配真实含"***"的密钥 |
| **L36** | feedback.js 使用 confirm() 阻塞式对话框 | 替换为自定义模态确认框，异步非阻塞，复用 modal-overlay 样式 |
| **L37** | feedback.js escapeHtml 每次创建临时对象 | 提取 HTML_ENTITY 常量为模块级；dashboard.html 同步修复 |

---

## 第35轮修复（兼容性与UI修复）

| 编号 | 问题 | 修复内容 |
|------|------|----------|
| **L15** | load_sessions 用 str(id(s)) 作 session_id 兜底 | 改为 str(uuid.uuid4()) 生成唯一ID，避免内存地址碰撞 |
| **M62/M63** | backdrop-filter 缺少 -webkit- 前缀 / 缺少 prefers-reduced-motion | 4处 backdrop-filter 前补充 -webkit-backdrop-filter，提升Safari兼容性 |
| **L57** | 跑团故事区域缺少回看入口提示 | 添加"↑ 滚动查看完整故事"提示，story-text为空时自动隐藏 |
| **L60** | 色彩对比度不足 | --text-dim 从 #9a8a7a 提亮至 #b8a898，通过WCAG AA级对比度标准 |

---

**文档创建**: 2026-07-05 | **源自**: 问题反馈与报告.md 第29轮 | **累计修复**: 250个
